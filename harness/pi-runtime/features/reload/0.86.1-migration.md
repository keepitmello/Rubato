# 0.86.1 patch judgments

Judged against the installed `@earendil-works/pi-coding-agent` 0.86.1 (the tree
`stage-runtime.mjs` verifies against; byte-identical to the 0.86.1 tarball).
Old-side oracle: the pristine 0.85.1 tarball. Every broken patch below was read
next to what changed around its anchor, not re-recorded on the hash alone.

`delete` count: 0. No 0.86 feature absorbs these intents — a postimage search
finds nothing in 0.86.1 for any patch in the whole 146, so absence proves
nothing; each keep/re-cut is argued from the 0.86.1 source instead.

12 patches, 9 were broken (2 anchor-dead).

| patch | judgment | reason |
|---|---|---|
| `dist/core/extensions/types.d.ts` | **re-cut** | `types-api-overload` anchor died: `ExtensionAPI.on` now returns `() => void` (the new unsubscribe handle) instead of `void`. |
| `dist/index.d.ts` | **re-cut** | `public-index-exports` anchor died: 0.86 added the `*Result` sibling exports, so `SessionBeforeForkEvent, SessionBeforeSwitchEvent` became `SessionBeforeForkEvent, SessionBeforeForkResult, SessionBeforeSwitchEvent`. |
| `dist/core/extensions/runner.js` | keep | 0.86 added the fail-closed `isUserBashEventResult` guard and `snapshotEventHandlers`. Our anchor is the cancellable-event list, untouched. |
| `dist/core/extensions/runner.d.ts` | keep | `CacheWarmingDecisionEvent` + `emitCacheWarmingDecision` added; our result-import and result-map anchors are intact. |
| `dist/core/extensions/index.d.ts` | keep | Our anchor already used the `*Result` form, which 0.86 kept. |
| `dist/core/agent-session.js` | keep | `reload()` is unchanged and 0.86 adds no reload veto, so the internal gate still carries its own value. Both anchors matched verbatim. |
| `dist/core/agent-session.d.ts` | keep | The `reload(options?)` declaration is unchanged. |
| `dist/modes/interactive/interactive-mode.js` | keep | All three anchors (precheck, deferred reset, race result) matched; the reload UI flow is unchanged. |
| `dist/modes/rpc/rpc-mode.js` | keep | Only `steer`/`follow_up` gained `{ source: "rpc" }`; our `abort`/`clear_queue` anchor is untouched. |
| `dist/modes/rpc/rpc-types.d.ts`, `rpc-client.js`, `rpc-client.d.ts` | keep (clean) | Unchanged files. |

Tests: `reload.test.mjs` 4/4.
