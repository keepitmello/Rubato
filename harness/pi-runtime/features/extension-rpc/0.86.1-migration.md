# 0.86.1 patch judgments

Judged against the installed `@earendil-works/pi-coding-agent` 0.86.1 (the tree
`stage-runtime.mjs` verifies against; byte-identical to the 0.86.1 tarball).
Old-side oracle: the pristine 0.85.1 tarball. Every broken patch below was read
next to what changed around its anchor, not re-recorded on the hash alone.

`delete` count: 0. No 0.86 feature absorbs these intents — a postimage search
finds nothing in 0.86.1 for any patch in the whole 146, so absence proves
nothing; each keep/re-cut is argued from the 0.86.1 source instead.

6 patches, 5 were broken (0 anchor-dead).

| patch | judgment | reason |
|---|---|---|
| `dist/core/extensions/loader.js` | keep | 0.86 deferred `VIRTUAL_MODULES` and added `pi.on()` unsubscribe; the extension-API object and our `rpc` insertion point before `events: {` are unchanged. |
| `dist/core/extensions/types.d.ts` | keep | Our `ExtensionAPI.rpc` and `Extension.rpcHandlers` anchors are intact; 0.86 only added cache-warming types and re-typed `BeforeAgentStartEvent`. |
| `dist/core/extensions/runner.js` | keep | Our `getCommand(name)` anchor and `requestRpc` insertion are intact. |
| `dist/core/extensions/runner.d.ts` | keep | The `getCommand` declaration is unchanged. |
| `dist/modes/rpc/rpc-mode.js` | keep | Only `steer`/`follow_up` gained `{ source: "rpc" }`. All four anchors (backpressure field, rebind, unknown-command default, shutdown) matched, and the 4-occurrence rebind count still holds. |
| `dist/modes/rpc/rpc-types.d.ts` | keep (clean) | Unchanged. |

Upstream added no extension-to-host RPC bridge in 0.86, so nothing was absorbed.

Tests: `extension-rpc.test.mjs` 2/2.
