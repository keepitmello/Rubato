# 0.86.1 patch judgments

Judged against the installed `@earendil-works/pi-coding-agent` 0.86.1 (the tree
`stage-runtime.mjs` verifies against; byte-identical to the 0.86.1 tarball).
Old-side oracle: the pristine 0.85.1 tarball. Every broken patch below was read
next to what changed around its anchor, not re-recorded on the hash alone.

`delete` count: 0. No 0.86 feature absorbs these intents — a postimage search
finds nothing in 0.86.1 for any patch in the whole 146, so absence proves
nothing; each keep/re-cut is argued from the 0.86.1 source instead.

11 patches, 10 were broken (1 anchor-dead).

| patch | judgment | reason |
|---|---|---|
| `dist/core/agent-session.js` | **re-cut** | `next-turn-live-window` anchor died: 0.86 renders the prompt from `systemPromptOptions` and carries prompt/tool deltas as messages, so the `context.systemPrompt` tail is gone. Re-anchored the same intent — swap the provider context's messages for the live history-notes window — on `context: { ...nextContext, tools }`. |
| `dist/core/agent-session.d.ts` | keep | 0.86 swapped `_systemPromptOverride` for `_runSystemPromptOptions`, dropped `_baseSystemPrompt`, added `_preparePromptAndToolLoadout`/`_restoreToolsFromTranscript`. Our anchors (`_resolveIdleWait` + revision fields, `_runDefaultCompaction`) are untouched and `applyCompaction`/`getMessageRevision` stay additive API we own. |
| `dist/core/messages.js` | keep | Only change is a `case "system":` added to `convertToLlm`'s switch. Our window carrier and the `map`/`filter` seam still mean the same thing. |
| `dist/core/sdk.js` | keep | 0.86 split `streamFn` into `buildRequestOptions`/`transformProviderPayload`/`handleProviderResponse` and added `CacheWarmer`; `transformContext` is verbatim. The admission gate still fails closed immediately before the provider call. |
| `dist/core/settings-manager.js` | keep | `getCompactionSettings(model)` gained a model param and per-model overrides. Our gate wraps it with `apply(this, args)`, so it stays signature-agnostic. |
| `dist/core/extensions/types.d.ts` | keep | `CacheWarmingDecisionEvent` joined the union and `BeforeAgentStartEvent` re-typed its prompt options. Our anchors (`CompactOptions`, context API/actions) are intact. |
| `dist/core/extensions/runner.js` | keep | 0.86 added `emitCacheWarmingDecision`, snapshotted handlers, and dropped `emitBeforeAgentStart`'s `systemPrompt` param. Our anchors (context-action fields, bind, `compact`) are intact. |
| `dist/core/extensions/runner.d.ts` | keep | Same neighbourhood as above; our anchor is the private field list. |
| `dist/core/extensions/index.d.ts` | keep | Only the export list grew; `AgentStartEvent, AgentToolResult` is still adjacent. |
| `dist/index.d.ts` | keep | Same. |
| `dist/core/messages.d.ts` | keep (clean) | Byte-identical between 0.85.1 and 0.86.1. |

Tests: `context-window.test.mjs` 11/11. Two assertions were adapted to the
0.86 transcript shape (`getCurrentSystemPrompt`/`getCurrentTools`, and the
leading system message is not conversation) — the guarded claims (window is the
only turn, no leak of the cut request or note body) are unchanged.
