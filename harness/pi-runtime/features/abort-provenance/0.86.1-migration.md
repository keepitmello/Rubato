# 0.86.1 patch judgments

Judged against the installed `@earendil-works/pi-coding-agent` 0.86.1 (the tree
`stage-runtime.mjs` verifies against; byte-identical to the 0.86.1 tarball).
Old-side oracle: the pristine 0.85.1 tarball. Every broken patch below was read
next to what changed around its anchor, not re-recorded on the hash alone.

`delete` count: 0. No 0.86 feature absorbs these intents — a postimage search
finds nothing in 0.86.1 for any patch in the whole 146, so absence proves
nothing; each keep/re-cut is argued from the 0.86.1 source instead.

5 patches, all 5 were broken (2 anchor-dead).

| patch | judgment | reason |
|---|---|---|
| `dist/core/agent-session.js` | **re-cut** | `post-run-stop` and `abort-entry` anchors died. 0.86 added its own `_agentRunAbortRequested` guards at the same decision points — partial absorption, not full: `#stopContinuation` is also armed by a provider-aborted `agent_end`, which no `abort()` call covers, so the sub-patch still carries unique value. Re-cut keeps upstream's guards verbatim and interleaves ours, sets `_agentRunAbortRequested` from `abort(source)` as upstream does, and consumes the stop-continuation up front — otherwise upstream's new early return skipped the consumption and leaked the flag into the next run, which made the retry-gap path hang forever. |
| `dist/core/extensions/types.d.ts` | **re-cut** | `types-api-overload` anchor died: `on` now returns `() => void`. |
| `dist/core/agent-session.d.ts` | keep | 0.86 added `_agentRunAbortRequested`/`_cacheWarmer` and `steer`/`followUp` options; our session-event union, `clearQueue` and `abort()` anchors are intact. |
| `dist/core/extensions/index.d.ts` | keep | Our `SessionAbortEvent` export anchor is intact. |
| `dist/index.d.ts` | keep | Same. |

Tests: `abort-provenance.test.mjs` 2/2 (the retry-gap and late-boundary phases
both exercise the re-cut paths).
