# 0.86.1 patch judgments

Judged against the installed `@earendil-works/pi-coding-agent` 0.86.1 (the tree
`stage-runtime.mjs` verifies against; byte-identical to the 0.86.1 tarball).
Old-side oracle: the pristine 0.85.1 tarball. Every broken patch below was read
next to what changed around its anchor, not re-recorded on the hash alone.

`delete` count: 0. No 0.86 feature absorbs these intents — a postimage search
finds nothing in 0.86.1 for any patch in the whole 146, so absence proves
nothing; each keep/re-cut is argued from the 0.86.1 source instead.

10 patches, 7 were broken (3 anchor-dead).

| patch | judgment | reason |
|---|---|---|
| `dist/core/extensions/loader.js` | **re-cut** | `api-lazy` anchor died: 0.86 added an object-parameter-schema guard inside `registerTool`, so the anchor's tail moved. Re-anchored with the guard included. |
| `dist/core/agent-session.js` | **re-cut** | `validation-import` anchor died: the pi-ai import line gained `getCurrentSystemMessage, retryDelayMs`. `validateToolArguments` still resolves from the pi-ai root (`export * from "./utils/validation.js"`). |
| `dist/index.d.ts` | **re-cut** | `type-export` anchor died: the public type export list now starts with `AfterProviderResponseEvent,`. |
| `dist/core/extensions/types.d.ts` | keep | `CacheWarmingDecisionEvent` and other 0.86 types joined; our anchors (`registerCommand`, `GetActiveToolsHandler`, `setLabel`/`getActiveTools`) are intact. |
| `dist/core/extensions/runner.js` | keep | Our bind-actions and pending-registration anchors are intact. |
| `dist/core/agent-session.d.ts` | keep | New private methods were added; our `_toolRegistry`/`_toolDefinitions` and `getToolDefinition` anchors are intact. |
| `dist/core/extensions/index.d.ts` | keep | Both export anchors intact. |
| `dist/core/extensions/types.js`, `extensions/index.js`, `index.js` | keep (clean) | Unchanged. |

Tests: `tool-execution.test.mjs` 3/3.
