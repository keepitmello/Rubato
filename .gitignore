# Dependencies and generated output
node_modules/
dist/
*.bun-build
.build-check/
.build-extension-test-shared-*/
harness/prompts/.build/

# Runtime state and evidence
.rubato/
.ulw/
plans/
.debugging
.debug-journal*.md
session-ses_*.md
pr-*.md
notepad.md

# Credentials and local environment
.env
.env.local
credentials.json
kiro-auth-token.json
oauth-success.html
test-injection/

# Editors and operating systems
.idea/
.vscode/
*.swp
*.swo
.DS_Store
Thumbs.db

# Logs and package-manager residue
*.log
npm-debug.log*
package-lock.json
# 워크스페이스 밖 하네스 패키지는 자기 의존성을 자기 잠금으로 설치한다.
# 잠금이 레포에 없으면 CI 의 `npm ci` 가 EUSAGE 로 죽는다.
!harness/pi-runtime/package-lock.json
!harness/rubato-pi/package-lock.json
yarn.lock

# Local agent workspaces
.claude/
.consult/
.outpost/
.dori-sync/
.dori/
.dys/
.playwright-mcp/

# 파이썬 바이트코드 — 스킬을 실행만 해도 생긴다. 커밋되면 설치본과 번들이
# 늘 달라 보여서 install-skills 가 그 스킬을 "사람이 고친 자리"로 보고 보존한다.
__pycache__/
*.pyc
.pytest_cache/
