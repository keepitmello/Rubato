diff --git a/dist/core/provider-timeout-retry.js b/dist/core/provider-timeout-retry.js
--- a/dist/core/provider-timeout-retry.js
+++ b/dist/core/provider-timeout-retry.js
@@ -37,6 +37,13 @@
     // dead upstream, and the watchdog still cancels a retry that outlives them)
     // while letting a slow-but-alive provider spend the budget it was configured
     // with across the full retry budget.
+    //
+    // Once the provider proves the request is alive (first stream event),
+    // `runBoundedRetryContinuation` disarms this watchdog: stream-start/idle
+    // guards own the request from then on. Leaving the wall clock armed through
+    // a live stream re-created the collapse — a retry that spent most of its
+    // start budget waiting, then began producing tokens, was aborted mid-stream
+    // at the watchdog deadline and surfaced as "Aborted after 1 retry attempt".
     return {
         options: {
             deferQueuedMessages: true,
@@ -46,23 +53,55 @@
         watchdogTimeoutMs: reconcileWatchdogTimeoutMs(streamRetryTimeoutMs, streamStartTimeoutMs),
     };
 }
-export async function runBoundedRetryContinuation({ continueRun, getActiveSignal, abortActive, timeoutMs, }) {
+/**
+ * Bound a retry continuation so a wedged `continue()` cannot hang forever before
+ * the provider stream guards attach. When `isLive` reports that the provider has
+ * produced its first event, the watchdog disarms permanently — stream-start and
+ * idle timeouts then own the request, including tool rounds after the stream.
+ */
+export async function runBoundedRetryContinuation({ continueRun, getActiveSignal, abortActive, timeoutMs, isLive, }) {
     const continuation = continueRun();
     const ownedSignal = getActiveSignal();
     if (timeoutMs === undefined || ownedSignal === undefined) {
         await continuation;
         return;
     }
-    const timer = setTimeout(() => {
+    let disarmed = false;
+    let timer;
+    let poll;
+    const disarm = () => {
+        if (disarmed)
+            return;
+        disarmed = true;
+        if (timer !== undefined)
+            clearTimeout(timer);
+        if (poll !== undefined)
+            clearInterval(poll);
+    };
+    timer = setTimeout(() => {
+        if (disarmed)
+            return;
+        // Late proof of life: a stream that crossed the deadline in the same
+        // tick as this timer must not be aborted — provider guards own it.
+        if (isLive?.()) {
+            disarm();
+            return;
+        }
         if (getActiveSignal() === ownedSignal) {
             abortActive();
         }
     }, timeoutMs);
+    if (isLive) {
+        poll = setInterval(() => {
+            if (isLive())
+                disarm();
+        }, 100);
+    }
     try {
         await continuation;
     }
     finally {
-        clearTimeout(timer);
+        disarm();
     }
 }
 //# sourceMappingURL=provider-timeout-retry.js.map
diff --git a/dist/core/provider-timeout-retry.d.ts b/dist/core/provider-timeout-retry.d.ts
--- a/dist/core/provider-timeout-retry.d.ts
+++ b/dist/core/provider-timeout-retry.d.ts
@@ -15,7 +15,12 @@
     getActiveSignal(): AbortSignal | undefined;
     abortActive(): void;
     timeoutMs: number | undefined;
+    /**
+     * When true, the provider has proved the retry request is alive. The
+     * watchdog disarms permanently so stream-start/idle guards own the rest.
+     */
+    isLive?: () => boolean;
 }
 export declare function createProviderTimeoutRetryPlan({ message, streamRetryTimeoutMs, timeoutMs, streamStartTimeoutMs, }: ProviderTimeoutRetryPlanInput): ProviderTimeoutRetryPlan;
-export declare function runBoundedRetryContinuation({ continueRun, getActiveSignal, abortActive, timeoutMs, }: BoundedRetryContinuation): Promise<void>;
+export declare function runBoundedRetryContinuation({ continueRun, getActiveSignal, abortActive, timeoutMs, isLive, }: BoundedRetryContinuation): Promise<void>;
 //# sourceMappingURL=provider-timeout-retry.d.ts.map
diff --git a/dist/core/agent-session.js b/dist/core/agent-session.js
--- a/dist/core/agent-session.js
+++ b/dist/core/agent-session.js
@@ -4605,6 +4605,9 @@
             await this._revalidateScheduledContinuationAdmission();
             if (this.agent.state.isStreaming)
                 return "taken-over";
+            // Sticky: once the provider emits, keep the watchdog disarmed through
+            // tool rounds where isStreaming flickers false.
+            let sawProviderLive = false;
             await runBoundedRetryContinuation({
                 continueRun: async () => {
                     if (this._scheduledContinuationRecompacted) {
@@ -4625,6 +4628,15 @@
                 getActiveSignal: () => this.agent.signal,
                 abortActive: () => this.agent.abort(),
                 timeoutMs: retryTimeoutMs,
+                isLive: () => {
+                    if (sawProviderLive)
+                        return true;
+                    if (this.agent.state.isStreaming) {
+                        sawProviderLive = true;
+                        return true;
+                    }
+                    return false;
+                },
             });
             return "continued";
         }
diff --git a/dist/modes/interactive/extension-error-format.js b/dist/modes/interactive/extension-error-format.js
--- a/dist/modes/interactive/extension-error-format.js
+++ b/dist/modes/interactive/extension-error-format.js
@@ -5,13 +5,27 @@
  * may decode `\u001b` escapes into live OSC/CSI sequences — so anything routed
  * to `Text` is sanitized first.
  */
+/**
+ * Display-only hint for provider stream stalls. Stored `errorMessage` values
+ * must keep their anchored shapes so retry classifiers still match; clarify
+ * only when painting the TUI.
+ */
+export function clarifyProviderStallError(message) {
+    if (/^Provider stream start timed out after \d+ms$/i.test(message)) {
+        return `${message}. Upstream sent no first event — often rate-limit or overload; wait and retry, or switch model/provider.`;
+    }
+    if (/^Idle timeout waiting for provider stream after \d+ms$/i.test(message)) {
+        return `${message}. The stream went silent mid-response — network stall or provider overload.`;
+    }
+    return message;
+}
 export function sanitizeTuiErrorMessage(value) {
-    return value
+    return clarifyProviderStallError(value
         .replace(/\u001b\][\s\S]*?(?:\u0007|\u001b\\|\u009c|$)/g, "")
         .replace(/(?:\u001b\[|\u009b)[0-?]*[ -/]*[@-~]/g, "")
         .replace(/\r\n?/g, "\n")
         .replace(/[\u0000-\u0008\u000b-\u001f\u007f-\u009f]/g, "")
-        .replace(/[ \t\f\v]+/g, " ");
+        .replace(/[ \t\f\v]+/g, " "));
 }
 /**
  * Headline for an extension-error line in the chat log.
diff --git a/dist/modes/interactive/components/assistant-render-descriptors.js b/dist/modes/interactive/components/assistant-render-descriptors.js
--- a/dist/modes/interactive/components/assistant-render-descriptors.js
+++ b/dist/modes/interactive/components/assistant-render-descriptors.js
@@ -1,6 +1,7 @@
 import { SERVER_FALLBACK_ABORTED_DIAGNOSTIC } from "@earendil-works/pi-ai";
 import { formatDuration } from "../../../utils/duration.js";
 import { formatProviderNativeBody, formatProviderNativeSummary } from "../../provider-native-rendering.js";
+import { sanitizeTuiErrorMessage } from "../extension-error-format.js";
 import { theme } from "../theme/theme.js";
 const SPACER_DESCRIPTOR = { kind: "spacer", text: "" };
 function assertNever(value) {
@@ -130,13 +131,13 @@
             const abortMessage = message.errorMessage && message.errorMessage !== "Request was aborted"
                 ? message.errorMessage
                 : "Operation aborted";
-            addError(abortMessage);
+            addError(sanitizeTuiErrorMessage(abortMessage));
             break;
         }
         case "error":
             if (!options.hasToolCalls &&
                 !message.diagnostics?.some((entry) => entry.type === SERVER_FALLBACK_ABORTED_DIAGNOSTIC))
-                addError(`Error: ${message.errorMessage || "Unknown error"}`);
+                addError(`Error: ${sanitizeTuiErrorMessage(message.errorMessage || "Unknown error")}`);
             break;
         case "pending":
         case "stop":
